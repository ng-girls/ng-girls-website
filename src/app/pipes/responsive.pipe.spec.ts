import { ResponsivePipe } from './responsive.pipe';

describe('ResponsivePipe', () => {
  it('create an instance', () => {
    const pipe = new ResponsivePipe();
    expect(pipe).toBeTruthy();
  });
});
